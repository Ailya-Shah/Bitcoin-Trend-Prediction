# ============================================================
#  Bitcoin Trend Prediction — Python dependencies
#  (Dashboard JS deps live in frontend/package.json -> `npm install`.)
# ============================================================

# ---- Core (needed everywhere) ----
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13

# ---- Statistics layer (notebooks 02–04, full rigor) ----
scipy>=1.11
statsmodels>=0.14          # ADF/KPSS, ACF/PACF, ARMA, Ljung-Box, ANOVA, OLS
arch>=7.0                  # GARCH / GJR-GARCH / EGARCH family

# ---- Machine learning (notebooks 06, 07, 09) ----
scikit-learn>=1.4          # classical models + the MLP neural-net benchmark + metrics
xgboost>=2.0

# ---- Data acquisition (scripts/) ----
requests>=2.31             # Binance price + Blockchain.com on-chain fetchers

# ---- Notebook pipeline runner (run_all.py) ----
jupyter>=1.0
nbformat>=5.9
nbconvert>=7.10
ipykernel>=6.29

# ---- OPTIONAL: real LSTM in notebook 07 ----
# Not required — notebook 07 falls back to a scikit-learn MLP if these are absent,
# so the whole pipeline runs without them. Install ONE only if you want the true
# sequence model (large download):
# tensorflow>=2.16          # Windows/Linux:  pip install tensorflow-cpu
# torch>=2.2                # PyTorch alternative
